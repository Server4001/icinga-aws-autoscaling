{
  "name": "icinga-aws-autoscaling",
  "version": "0.1.0",
  "description": "Dynamic management of AWS Auto-Scaling Group inventory in Icinga.",
  "main": "index.js",
  "scripts": {
    "test": "mocha tests/**/*.js",
    "test:coverage": "istanbul cover _mocha -- -R tap \"./tests/**/*.js\" > ./test.tap && istanbul report clover",
    "build": "zip -r ./build/icinga-aws-autoscaling-$(date +%s).zip . -x \"*.git*\" -x \"*.idea*\" -x \"*config.json.example*\" -x \"*examples*\" -x \"*build*\" -x \"*tests*\""
  },
  "author": "Brice Bentler",
  "license": "MIT",
  "dependencies": {
    "request": "2.81.0"
  },
  "devDependencies": {
    "aws-sdk": "2.58.0",
    "chai": "3.5.0",
    "istanbul": "0.4.5",
    "mocha": "3.2.0"
  }
}
